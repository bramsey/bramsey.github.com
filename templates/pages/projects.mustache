<section id="projects">
  <div class="container group">
    {{#projectItems}}
    <a class="preview" href="{{id}}.html">
      <img src="{{cover_url}}" alt="{{title}}"></img>
    </a>
    {{/projectItems}}
  </div>
</section>
